# NOT live-migratable: RWO disk. Check the LiveMigratable condition on the VMI:
#   oc get vmi mig-rwo-vm -n migr-lab -o jsonpath='{.status.conditions[?(@.type=="LiveMigratable")]}'
apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: mig-rwo-vm
  namespace: migr-lab
spec:
  runStrategy: Always
  dataVolumeTemplates:
    - metadata:
        name: mig-rwo-vm-root
      spec:
        source:
          registry:
            url: docker://quay.io/containerdisks/fedora:latest
        storage:
          accessModes:
            - ReadWriteOnce
          volumeMode: Filesystem
          resources:
            requests:
              storage: 10Gi
  template:
    metadata:
      labels:
        app: mig-rwo-vm
    spec:
      domain:
        cpu:
          cores: 1
        devices:
          disks:
            - name: rootdisk
              disk:
                bus: virtio
            - name: cloudinitdisk
              disk:
                bus: virtio
          interfaces:
            - name: default
              masquerade: {}
        resources:
          requests:
            memory: 2Gi
      networks:
        - name: default
          pod: {}
      terminationGracePeriodSeconds: 30
      volumes:
        - name: rootdisk
          dataVolume:
            name: mig-rwo-vm-root
        - name: cloudinitdisk
          cloudInitNoCloud:
            userData: |
              #cloud-config
              user: cloud-user
              password: lab-pass-123
              chpasswd:
                expire: false
              ssh_pwauth: true
