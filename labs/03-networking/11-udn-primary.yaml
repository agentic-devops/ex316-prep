apiVersion: k8s.ovn.org/v1
kind: UserDefinedNetwork
metadata:
  name: udn-primary
  namespace: udn-lab
spec:
  topology: Layer2
  layer2:
    role: Primary
    subnets:
      - 10.100.0.0/24
    ipam:
      lifecycle: Persistent
